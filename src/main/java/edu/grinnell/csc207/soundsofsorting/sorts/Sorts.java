package edu.grinnell.csc207.soundsofsorting.sorts;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import edu.grinnell.csc207.soundsofsorting.sortevents.CompareEvent;
import edu.grinnell.csc207.soundsofsorting.sortevents.SortEvent;
import edu.grinnell.csc207.soundsofsorting.sortevents.SwapEvent;

/**
 * A collection of sorting algorithms.
 */
public class Sorts {
    /**
     * Swaps indices <code>i</code> and <code>j</code> of array <code>arr</code>.
     * @param <T> the carrier type of the array
     * @param arr the array to swap
     * @param i the first index to swap
     * @param j the second index to swap
     */
    public static <T> void swap(T[] arr, int i, int j) {
        T tmp = arr[i];
        arr[i] = arr[j];
        arr[j] = tmp;
    }

    /**
     * Sorts the array according to the bubble sort algorithm:
     * <pre>
     * [ unprocessed | i largest elements in order ]
     * </pre>
     * @param <T> the carrier type of the array
     * @param arr the array to sort
     * @return the sort events generated by this sort
     */
    public static <T extends Comparable<? super T>> List<SortEvent<Integer>> bubbleSort(T[] arr) {
        List<SortEvent<Integer>> l = new ArrayList<SortEvent<Integer>>();
            
        for(int i = arr.length; i>0; i--) {
            for(int j=0; j < i-1; j++) {
                List<Integer> compInd = new ArrayList<Integer>();
                CompareEvent<Integer> compEvent = new CompareEvent<>(compInd);
                l.add(compEvent);
                if(arr[j].compareTo(arr[j+1]) > 0) {
                    List<Integer> swapInd = new ArrayList<Integer>();
                    SwapEvent<Integer> swapEvent = new SwapEvent<>(swapInd);
                    l.add(swapEvent);
                    swap(arr, j, j+1);
                }
            }
        }
        return l;
    }


    /**
     * Sorts the array according to the selection sort algorithm:
     * <pre>
     * [ i smallest elements in order | unprocessed ]
     * </pre>
     * @param <T> the carrier type of the array
     * @param arr the array to sort
     * @return the sort events generated by this sort
     */
    public static <T extends Comparable<? super T>> List<SortEvent<Integer>> selectionSort(
            T[] arr) {
        for(int i=0; i<arr.length; i++) {
            int max = i;
            for(int j=i+1; j<arr.length; j++) {
                if(arr[j].compareTo(arr[max]) < 0) {
                    max = j;
                }
            }
            swap(arr, i, max);
        }
        return null;
    }

    /**
     * Sorts the array according to the insertion sort algorithm:
     * <pre>
     * [ i elements in order | unprocessed ] 
     * </pre>
     * @param <T> the carrier type of the array
     * @param arr the array to sort
     * @return the sort events generated by this sort
     */
    public static <T extends Comparable<? super T>> List<SortEvent<Integer>> insertionSort(
            T[] arr) {
        for(int i=0; i<arr.length; i++) {
            for(int j=i; j>0; j--) {
                if(arr[j].compareTo(arr[j-1]) < 0) {
                    swap(arr, j, j-1);
                }
            }
        }
        return null;
    }

    public static <T extends Comparable<? super T>> void merge(T[] arr, T[] tempArr, int left, int right, int end) {
        int i = left;
        int j = right;
        for(int k = left; k < end; k++) {
            if(i >= right) {
                tempArr[k] = arr[j];
                j++;
            } else if(j >= end) {
                tempArr[k] = arr[i];
                i++;
            } else {
                if(arr[i].compareTo(arr[j]) <= 0) {
                    tempArr[k] = arr[i];
                    i++;
                } else {
                    tempArr[k] = arr[j];
                    j++;
                }
            }
        }
        System.arraycopy(tempArr, 0, arr, 0, end);
    }

    public static <T extends Comparable<? super T>> void mergeSortHelper(T[] arr, T[] tempArr, int start, int end) {
        int left = start;
        int right = (end+start) / 2;
        if(left != right) {
            mergeSortHelper(arr, tempArr, left, right);
            mergeSortHelper(arr, tempArr, right, end);
        }
        merge(arr, tempArr, left, right, end);
    }

    /**
     * Sorts the array according to the merge sort algorithm.
     * @param <T> the carrier type of the array
     * @param arr the array to sort
     * @return the sort events generated by this sort
     */
    public static <T extends Comparable<? super T>> List<SortEvent<Integer>> mergeSort(
            T[] arr) {
        T[] tempArr = Arrays.copyOf(arr, arr.length);
        mergeSortHelper(arr, tempArr, 0, arr.length);
        return null;
    }


    /**
     * Sorts the array according to the quick sort algorithm.
     * @param <T> the carrier type of the array
     * @param arr the array to sort
     * @return the sort events generated by this sort
     */
    public static <T extends Comparable<? super T>> List<SortEvent<Integer>> quickSort(T[] arr) {
        //TODO: implement for corrections
        mergeSort(arr);

        return null;
    }

    public static <T> void eventSort(T[] l, List<SortEvent<T>> events) {
        for(int i=0; i<events.size(); i++) {
            events.get(i).apply(l);
        }
    } 

}
